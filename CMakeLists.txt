# generated by clion
cmake_minimum_required(VERSION 3.13)
project(OpenGLProjects)
set(CMAKE_CXX_STANDARD 17)
add_library(myLogicalExtLib SHARED IMPORTED)

# my CMakeLists

# Set a path variable
set(OPENGL_FILE .)

# include header files
include_directories(${OPENGL_FILE}/include)

# link library
link_directories(${OPENGL_FILE}/lib)

# add a subdirectory (It seems doesn't matter?)
add_library(GLAD "include/glad/glad.c")
add_library(STB "include/stb_image/stb_image.cpp")

# Put all libraries together
set(LIBS glfw3 GLAD STB)
#
## search for all source file(.cpp, .c)
#aux_source_directory(Source SOURCE)
#add_executable(OpenGLProjects ${SOURCE})
# link main.cpp
#target_link_libraries(OpenGLProjects  ${LIBS})

file(GLOB_RECURSE SOURCE
        "Source/*.h"
        "Source/*.h"
        "Source/*.cpp")
add_executable(GC ${SOURCE})
target_link_libraries(GC ${LIBS})


